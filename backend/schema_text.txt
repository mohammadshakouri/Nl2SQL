__MigrationHistory Table: تاریخچه مهاجرت، شناسه‌ها، مدل‌ها، نسخه‌ها. شامل ستون های MigrationId, ContextKey. استفاده برای ردیابی تغییرات، ورژن‌بندی، مهاجرت‌ها، سوابق سیستم.
 | 
AspNetRoles Table: نقش ها، شناسنامه، کاربر، دسترسی. شامل ستون های Id. استفاده برای مدیریت نقش، احراز هویت، مجوزها.
 | 
AspNetUserClaims Table: شناسه کاربر، نوع ادعا، مقدار ادعا. شامل ستون های Id. استفاده برای مدیریت مجوزها، پروفایل کاربری، اطلاعات اعتباری.
 | 
AspNetUserLogins Table: لاگ ورود، ارائه‌دهنده، شناسه کاربر. شامل ستون های LoginProvider, ProviderKey, UserId. استفاده برای ردیابی ورود، احراز هویت، مدیریت حساب کاربری.
 | 
AspNetUserRoles Table: کاربر، نقش، ارتباط، نگاشت، هویت. شامل ستون های UserId, RoleId. استفاده برای تخصیص نقش، مدیریت دسترسی، تعیین سطح کاربری.
 | 
AspNetUsers Table: کاربران، اطلاعات احراز هویت، رمز عبور، شماره تلفن. شامل ستون های Id. استفاده برای مدیریت کاربران، احراز هویت، امنیت حساب، پروفایل کاربری.
 | 
Book Table: کتاب، شناسه، عنوان، نویسنده، قیمت. شامل ستون های BookID. استفاده برای مدیریت، موجودی، اطلاعات کتاب، فروش.
 | 
Category Table: دسته‌بندی‌ها، نام‌ها، شناسه‌ها. شامل ستون های CategoryID. استفاده برای طبقه‌بندی، سازماندهی، ردیابی، دسته‌بندی اصلی.
 | 
Error Table: شناسه خطا، کد، نوع، توضیحات، مسیر، تاریخ. شامل ستون های ErrorID. استفاده برای ثبت خطا، ردیابی اشکال، گزارش‌دهی، مدیریت خطا.
 | 
GateWayes Table: شناسه، نام، دروازه، ارتباطات. شامل ستون های GateWayID. استفاده برای مدیریت، اتصال، پورتال، سیستم.
 | 
OrderDetails Table: جزئیات سفارش، شناسه، کتاب، قیمت. شامل ستون های OrderDetailID. استفاده برای سفارشات، اقلام، کتاب، قیمت‌گذاری.
 | 
Orders Table: سفارشات، تاریخ، مبلغ، اطلاعات پرداخت، شناسه. شامل ستون های OrderID. استفاده برای ثبت سفارش، پیگیری تراکنش، اطلاعات مشتری، پرداخت مالی.
 | 
SiteSetting Table: تنظیمات سایت، شناسه، مقدار. شامل ستون های SettingId. استفاده برای پیکربندی، پارامتر، تنظیمات عمومی.
 | 
Tokens Table: شناسه توکن، مقدار، زمان ایجاد، کاربر. شامل ستون های TokenID. استفاده برای مدیریت توکن، احراز هویت، سوابق کاربری.
 | 
__MigrationHistory.MigrationId Column: شناسه، مهاجرت، تاریخچه، مدل, نوع داده varchar, استفاده برای کلید اصلی، جستجو، فیلتر، بررسی.
 | 
__MigrationHistory.ContextKey Column: کلید، شناسه‌، مهاجرت، مدل, نوع داده varchar, استفاده برای کلید اصلی، جستجو، فیلتر، پیوند.
 | 
__MigrationHistory.Model Column: مدل، شناسه‌سازی، تاریخچه، مهاجرت, نوع داده varbinary, استفاده برای فیلتر، جستجو، بررسی، مقایسه.
 | 
__MigrationHistory.ProductVersion Column: نسخه محصول، شناسه‌سازی، تاریخچه، مدل, نوع داده varchar, استفاده برای فیلتر، مقایسه، مرتب‌سازی، بررسی.
 | 
AspNetRoles.Id Column: شناسه، نقش، منحصر، اصلی, نوع داده varchar, استفاده برای کلید اصلی، فیلتر، پیوند، جستجو.
 | 
AspNetRoles.Name Column: نام، نقش، شناسنامه، کاربر, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، مرتب‌سازی.
 | 
AspNetUserClaims.Id Column: شناسه، کاربر، ادعا، کلید اصلی, نوع داده integer, استفاده برای فیلتر، پیوند، جستجو، مرتب‌سازی.
 | 
AspNetUserClaims.UserId Column: شناسه کاربر، شناسایی، مالکیت، کاربر, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو، ارتباط.
 | 
AspNetUserClaims.ClaimType Column: نوع ادعا، مشخصه کاربر، رشته متنی, نوع داده varchar, استفاده برای فیلتر، جستجو، مقایسه، مرتب‌سازی.
 | 
AspNetUserClaims.ClaimValue Column: مقدار ادعا، رشته متنی، داده کاربر, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، مقایسه.
 | 
AspNetUserLogins.LoginProvider Column: ارائه‌دهنده، منبع ورود، شناسه، کاربر, نوع داده varchar, استفاده برای فیلتر، جستجو، کلید اصلی، پیوند.
 | 
AspNetUserLogins.ProviderKey Column: کلید ارائه‌دهنده، منبع ورود، منحصر به فرد, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو، کلید اصلی.
 | 
AspNetUserLogins.UserId Column: شناسه کاربر، کلید اصلی، منحصر به فرد, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو، مرتب‌سازی.
 | 
AspNetUserRoles.UserId Column: شناسه کاربر، نقش، نگاشت، هویت, نوع داده varchar, استفاده برای کلید اصلی، فیلتر، پیوند، جستجو.
 | 
AspNetUserRoles.RoleId Column: شناسه نقش، کلید اصلی، کاربر، نقش, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو، تعیین.
 | 
AspNetUsers.Id Column: شناسه، کلید اصلی، کاربر، احراز هویت, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو، مرتب‌سازی.
 | 
AspNetUsers.Email Column: ایمیل، آدرس، شناسایی، کاربر, نوع داده varchar, استفاده برای فیلتر، جستجو، مقایسه، اتصال.
 | 
AspNetUsers.EmailConfirmed Column: تایید ایمیل، وضعیت، احراز هویت, نوع داده boolean, استفاده برای فیلتر، بررسی، شرط‌بندی.
 | 
AspNetUsers.PasswordHash Column: هش رمز عبور، اطلاعات احراز هویت، رشته متنی, نوع داده varchar, استفاده برای فیلتر، جستجو، مقایسه، ذخیره.
 | 
AspNetUsers.SecurityStamp Column: مهر امنیتی، تغییر رمز، منحصربه‌فرد, نوع داده varchar, استفاده برای فیلتر، مقایسه، بررسی تغییر.
 | 
AspNetUsers.PhoneNumber Column: شماره تلفن، تماس، ارتباط، احراز هویت, نوع داده varchar, استفاده برای فیلتر، جستجو، مرتب‌سازی، اتصال.
 | 
AspNetUsers.PhoneNumberConfirmed Column: تایید شماره تلفن، وضعیت تأیید، بولی, نوع داده boolean, استفاده برای فیلتر، بررسی، شرط‌بندی.
 | 
AspNetUsers.TwoFactorEnabled Column: احراز هویت، دو مرحله‌ای، فعال، وضعیت, نوع داده boolean, استفاده برای فیلتر، شرط، بررسی، مقایسه.
 | 
AspNetUsers.LockoutEndDateUtc Column: تاریخ قفل شدن، زمان محدودیت، احراز هویت, نوع داده datetime, استفاده برای فیلتر، مقایسه تاریخ، مرتب‌سازی.
 | 
AspNetUsers.LockoutEnabled Column: قفل حساب، فعال/غیرفعال، امنیت ورود, نوع داده boolean, استفاده برای فیلتر، شرط، بررسی وضعیت.
 | 
AspNetUsers.AccessFailedCount Column: تعداد تلاش ناموفق ورود، شمارش، احراز هویت, نوع داده integer, استفاده برای فیلتر، شمارش، مقایسه، مرتب‌سازی.
 | 
AspNetUsers.UserName Column: نام کاربری، شناسایی، کاربر، احراز هویت, نوع داده varchar, استفاده برای فیلتر، جستجو، پیوند، مرتب‌سازی.
 | 
AspNetUsers.IsSimacUser Column: کاربر، سیم‌کارت، احراز هویت، منطقی, نوع داده boolean, استفاده برای فیلتر، شرط، بررسی، مقایسه.
 | 
AspNetUsers.ProvinceCode Column: کد استان، کد منطقه‌ای، اطلاعات جغرافیایی, نوع داده integer, استفاده برای فیلتر، مرتب‌سازی، پیوند، جستجو.
 | 
Book.BookID Column: شناسه کتاب، کلید اصلی، منحصربه‌فرد, نوع داده integer, استفاده برای فیلتر، پیوند، مرتب‌سازی.
 | 
Book.Title Column: عنوان کتاب، نام اثر، متن اصلی, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش.
 | 
Book.CategoryID Column: شناسه، دسته‌بندی، عدد صحیح, نوع داده integer, استفاده برای فیلتر، پیوند، مرتب‌سازی.
 | 
Book.ISBN Column: شناسه کتاب، منحصربه‌فرد، کتابشناسی, نوع داده varchar, استفاده برای جستجو، فیلتر، پیوند، مرتب‌سازی.
 | 
Book.Author Column: نویسنده، نام، خالق، نویسنده کتاب, نوع داده varchar, استفاده برای فیلتر، جستجو، مرتب‌سازی، پیوند.
 | 
Book.Price Column: قیمت، مبلغ، پول، هزینه, نوع داده decimal, استفاده برای فیلتر، مرتب‌سازی، مقایسه، محاسبه.
 | 
Book.Status Column: وضعیت، فعال، غیرفعال، منطقی, نوع داده boolean, استفاده برای فیلتر، شرط، مقایسه، مرتب‌سازی.
 | 
Category.CategoryID Column: شناسه، دسته‌بندی، اصلی، عدد صحیح, نوع داده integer, استفاده برای کلید اصلی، فیلتر، پیوند، مرتب‌سازی.
 | 
Category.Name Column: نام، عنوان، دسته‌بندی، متن, نوع داده varchar, استفاده برای فیلتر، نمایش، مرتب‌سازی، جستجو.
 | 
Error.ErrorID Column: شناسه خطا، کلید اصلی، منحصربه‌فرد, نوع داده decimal, استفاده برای فیلتر، مرتب‌سازی، پیوند.
 | 
Error.ErrorCode Column: کد خطا، شناسه، نوع، توضیحات, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، مرتب‌سازی.
 | 
Error.ErrorVersion Column: نسخه خطا، رشته متنی، ورژن, نوع داده varchar, استفاده برای فیلتر، مقایسه، مرتب‌سازی.
 | 
Error.ErrorDes Column: شرح خطا، توضیحات، متن، پیام, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، گزارش‌گیری.
 | 
Error.ErrorType Column: نوع خطا، کد، عدد، شناسایی, نوع داده integer, استفاده برای فیلتر، جستجو، مقایسه، مرتب‌سازی.
 | 
Error.ErrorPath Column: مسیر خطا، آدرس، مسیردهی، متن, نوع داده varchar, استفاده برای فیلتر، جستجو، مقایسه، شرط‌بندی.
 | 
Error.ErrorTrace Column: ردیابی خطا، متن، جزئیات، مسیر, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، بررسی.
 | 
Error.ErrorUserID Column: شناسه کاربر، خطای مرتبط، شناسایی کاربر, نوع داده varchar, استفاده برای فیلتر، جستجو، پیوند، محدود کردن.
 | 
Error.ErrorUserIP Column: آی پی کاربر، منبع خطا، شناسایی کاربر, نوع داده varchar, استفاده برای فیلتر، جستجو، گزارش‌گیری.
 | 
Error.ErrorDate Column: تاریخ وقوع خطا، زمان ثبت، زمان وقوع, نوع داده datetime, استفاده برای فیلتر، مرتب‌سازی، مقایسه تاریخ.
 | 
Error.ErrorSystemID Column: شناسه سیستم خطا، شناسه‌دهی، منبع خطا, نوع داده integer, استفاده برای فیلتر، جستجو، پیوند، شرط‌بندی.
 | 
GateWayes.GateWayID Column: شناسه، دروازه، کلیدی، منحصر, نوع داده integer, استفاده برای کلید اصلی، فیلتر، پیوند، جستجو.
 | 
GateWayes.GateWayName Column: نام، دروازه، شناسایی، متن, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، مرتب‌سازی.
 | 
OrderDetails.OrderDetailID Column: شناسه، جزئیات، سفارش، کلید اصلی, نوع داده integer, استفاده برای فیلتر، پیوند، جستجو، شناسایی.
 | 
OrderDetails.OrderID Column: شناسه سفارش، جزئیات، ارتباط، کلید, نوع داده integer, استفاده برای فیلتر، پیوند، جستجو، مرتب‌سازی.
 | 
OrderDetails.BookID Column: شناسه کتاب، کد کتاب، شناسه‌دهی, نوع داده integer, استفاده برای فیلتر، پیوند، جستجو، مرتب‌سازی.
 | 
OrderDetails.Price Column: قیمت، مبلغ، هزینه، واحد, نوع داده decimal, استفاده برای فیلتر، محاسبه، مرتب‌سازی، گزارش‌گیری.
 | 
Orders.OrderID Column: شناسه سفارش، کلیدی، منحصر، شناسایی, نوع داده integer, استفاده برای کلید اصلی، فیلتر، پیوند، جستجو.
 | 
Orders.UserId Column: شناسه کاربر، مالک سفارش، هویت, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو.
 | 
Orders.OrderDate Column: تاریخ سفارش، زمان ثبت، تاریخ وقوع, نوع داده datetime, استفاده برای فیلتر، مرتب‌سازی، مقایسه تاریخ.
 | 
Orders.terminalNumber Column: شماره ترمینال، شناسایی پرداخت، متن, نوع داده varchar, استفاده برای فیلتر، جستجو، مقایسه، مرتب‌سازی.
 | 
Orders.responseCode Column: کد پاسخ، وضعیت تراکنش، متن کوتاه, نوع داده varchar, استفاده برای فیلتر، جستجو، بررسی وضعیت.
 | 
Orders.paymentId Column: شناسه پرداخت، اطلاعات پرداخت، پیوند، منحصربه‌فرد, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو، مرتب‌سازی.
 | 
Orders.RequestId Column: شناسه سفارش، شناسایی، منحصر به فرد, نوع داده varchar, استفاده برای فیلتر، جستجو، پیوند، مرتب‌سازی.
 | 
Orders.rrn Column: شناسه، ردیابی، منحصر، سفارش, نوع داده varchar, استفاده برای فیلتر، جستجو، پیوند، مرتب‌سازی.
 | 
Orders.amount Column: مبلغ، قیمت، ارزش، پول, نوع داده decimal, استفاده برای فیلتر، محاسبه، مرتب‌سازی، گزارش.
 | 
Orders.maskedPan Column: شماره کارت، اطلاعات پرداخت، شناسایی مشتری, نوع داده varchar, استفاده برای فیلتر، جستجو، امنیت، نمایش.
 | 
Orders.traceNo Column: شماره پیگیری، شناسایی، ردیابی، سفارش, نوع داده varchar, استفاده برای فیلتر، جستجو، یکتایی، پیوند.
 | 
Orders.IsComittedBySimac Column: تعهد، سیماک، منطقی، وضعیت, نوع داده boolean, استفاده برای فیلتر، شرط، بررسی، مقایسه.
 | 
Orders.FullName Column: نام کامل، مشتری، شناسنامه, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش.
 | 
Orders.NationalId Column: کد ملی، شناسایی، مشتری، منحصر به فرد, نوع داده varchar, استفاده برای فیلتر، جستجو، پیوند، مرتب‌سازی.
 | 
Orders.IsFinalizedPayment Column: پرداخت نهایی، وضعیت پرداخت، تایید تراکنش, نوع داده boolean, استفاده برای فیلتر، شرط، بررسی وضعیت.
 | 
SiteSetting.SettingId Column: شناسه، تنظیمات، سایت، کلیدی, نوع داده integer, استفاده برای کلید اصلی، فیلتر، جستجو، اتصال.
 | 
SiteSetting.Name Column: نام، تنظیمات، سایت، شناسه, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، مقایسه.
 | 
SiteSetting.Value Column: مقدار، عدد صحیح، تنظیمات سایت, نوع داده integer, استفاده برای فیلتر، جستجو، مقایسه، مرتب‌سازی.
 | 
Tokens.TokenID Column: شناسه توکن، کلید اصلی، منحصربه‌فرد, نوع داده integer, استفاده برای فیلتر، پیوند، مرتب‌سازی، جستجو.
 | 
Tokens.TokenValue Column: شناسه توکن، مقدار، زمان، کاربر, نوع داده varchar, استفاده برای فیلتر، جستجو، نمایش، مرتب‌سازی.
 | 
Tokens.CreateTime Column: زمان ایجاد، تاریخ، ساعت، ثبت وقایع, نوع داده datetime, استفاده برای فیلتر، مرتب‌سازی، مقایسه، محدوده زمانی.
 | 
Tokens.UserId Column: شناسه کاربر، مالکیت، هویت, نوع داده varchar, استفاده برای فیلتر، پیوند، جستجو، محدود کردن.
 | 
Relation: AspNetUserClaims.UserId ↔ AspNetUsers.Id. نوع رابطه many-to-one. استفاده برای کاربر، شناسه، ارتباط، عضویت.
 | 
Relation: AspNetUserLogins.UserId ↔ AspNetUsers.Id. نوع رابطه many-to-one. استفاده برای کاربر، شناسه، ارتباط، ورود، حساب.
 | 
Relation: AspNetUserRoles.RoleId ↔ AspNetRoles.Id. نوع رابطه many-to-one. استفاده برای نقش، کاربر، ارتباط، تعیین نقش.
 | 
Relation: AspNetUserRoles.UserId ↔ AspNetUsers.Id. نوع رابطه many-to-one. استفاده برای کاربر، نقش، ارتباط، عضویت.
 | 
Relation: OrderDetails.BookID ↔ Book.BookID. نوع رابطه many-to-one. استفاده برای جزئیات سفارش، کتاب، ارتباط، شناسایی کتاب.
 | 
Relation: OrderDetails.OrderID ↔ Orders.OrderID. نوع رابطه many-to-one. استفاده برای سفارش جزئیات، سفارش اصلی، ارتباط، شناسایی سفارش.
 | 
Relation: Orders.OrderID ↔ Orders.OrderID. نوع رابطه many-to-one. استفاده برای پیوند سفارشات، شناسایی، کلید اصلی، یکتا.
 | 
Relation: Tokens.UserId ↔ AspNetUsers.Id. نوع رابطه many-to-one. استفاده برای کاربر، شناسه، ارتباط، مطابقت.