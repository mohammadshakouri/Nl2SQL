AspNetUsers Table: کاربران، اطلاعات احراز هویت، ایمیل، رمز عبور، شماره تلفن. شامل ستون های Id. 
 | 
Book Table: کتاب، شناسه، عنوان، نویسنده، قیمت، وضعیت. شامل ستون های BookID. 
 | 
Category Table: شناسه دسته، نام دسته، دسته‌بندی‌ها. شامل ستون های CategoryID. 
 | 
Error Table: شناسه خطا، کد، نوع، توضیحات، تاریخ وقوع. شامل ستون های ErrorID. 
 | 
GateWayes Table: شناسه، نام، دروازه‌ها، ارتباطات، سیستم. شامل ستون های GateWayID. 
 | 
OrderDetails Table: جزئیات سفارش، شناسه، کتاب، قیمت. شامل ستون های OrderDetailID. 
 | 
Orders Table: سفارشات، اطلاعات خرید، تاریخ، مبلغ، مشتری. شامل ستون های OrderID. 
 | 
SiteSetting Table: تنظیمات سایت، شناسه، نام، مقدار. شامل ستون های SettingId. 
 | 
Tokens Table: شناسه توکن، مقدار، زمان ایجاد، کاربر. شامل ستون های TokenID. 
 | 
AspNetUsers.Id Column: شناسه کاربری، کلید اصلی، منحصر به فرد, نوع داده varchar.
 | 
AspNetUsers.Email Column: ایمیل، آدرس الکترونیکی، شناسایی کاربر, نوع داده varchar.
 | 
AspNetUsers.EmailConfirmed Column: تایید ایمیل، وضعیت، باینری، احراز هویت, نوع داده boolean.
 | 
AspNetUsers.PasswordHash Column: رمز عبور، هش، امنیتی، داده، متنی, نوع داده varchar.
 | 
AspNetUsers.SecurityStamp Column: مهر امنیتی، تغییر رمز، به‌روزرسانی، هش, نوع داده varchar.
 | 
AspNetUsers.PhoneNumber Column: شماره تلفن، ارتباط، تماس، موبایل, نوع داده varchar.
 | 
AspNetUsers.PhoneNumberConfirmed Column: شماره تلفن، تأیید، احراز هویت، باینری, نوع داده boolean.
 | 
AspNetUsers.TwoFactorEnabled Column: احراز هویت دو مرحله‌ای، فعال بودن، تایید امنیتی, نوع داده boolean.
 | 
AspNetUsers.LockoutEndDateUtc Column: تاریخ، قفل شدن، زمان، احراز هویت, نوع داده datetime.
 | 
AspNetUsers.LockoutEnabled Column: قفل حساب، فعال سازی، محدودیت ورود, نوع داده boolean.
 | 
AspNetUsers.AccessFailedCount Column: تعداد تلاش ناموفق ورود، احراز هویت، شکست ورود, نوع داده integer.
 | 
AspNetUsers.UserName Column: نام کاربری، شناسایی، کاربر، یونیک, نوع داده varchar.
 | 
AspNetUsers.IsSimacUser Column: کاربر سیم‌کارت، احراز هویت، فعال بودن، منطقی, نوع داده boolean.
 | 
AspNetUsers.ProvinceCode Column: کد استان، موقعیت جغرافیایی، عدد صحیح, نوع داده integer.
 | 
Book.BookID Column: Column BookID, نوع داده integer.
 | 
Book.Title Column: عنوان کتاب، نام اثر، عنوان اصلی, نوع داده varchar.
 | 
Book.CategoryID Column: شناسه دسته‌بندی، عدد صحیح، ارتباطی, نوع داده integer.
 | 
Book.ISBN Column: شناسه کتاب، ISBN، منحصر، شناسایی, نوع داده varchar.
 | 
Book.Author Column: نویسنده، نام، نویسنده کتاب, نوع داده varchar.
 | 
Book.Price Column: قیمت، مبلغ، پول، عددی, نوع داده decimal.
 | 
Book.Status Column: کتاب، موجود، در دسترس، خرید, نوع داده boolean.
 | 
Category.CategoryID Column: شناسه، کلید اصلی، شناسه‌دهی دسته, نوع داده integer.
 | 
Category.Name Column: نام دسته، عنوان، شناسه‌گذاری، متن, نوع داده varchar.
 | 
Error.ErrorID Column: شناسه خطا، کلید اصلی، منحصر به فرد, نوع داده decimal.
 | 
Error.ErrorCode Column: کد خطا، شناسایی، نوع خطا، مقدار متنی, نوع داده varchar.
 | 
Error.ErrorVersion Column: نسخه خطا، تغییرات، ورژن، شناسایی, نوع داده varchar.
 | 
Error.ErrorDes Column: شرح خطا، توضیحات، متن، پیام خطا, نوع داده varchar.
 | 
Error.ErrorType Column: نوع خطا، کد، شناسایی، دسته‌بندی, نوع داده integer.
 | 
Error.ErrorPath Column: مسیر خطا، مسیر وقوع، مسیر سیستم, نوع داده varchar.
 | 
Error.ErrorTrace Column: ردیابی خطا، مسیر، جزئیات فنی، اشکال‌زدایی, نوع داده varchar.
 | 
Error.ErrorUserID Column: شناسه کاربر، خطای مربوط به، کاربر، شناسایی, نوع داده varchar.
 | 
Error.ErrorUserIP Column: آی پی کاربر، منبع خطا، آدرس اینترنت, نوع داده varchar.
 | 
Error.ErrorDate Column: تاریخ وقوع خطا، زمان وقوع، ثبت زمان, نوع داده datetime.
 | 
Error.ErrorSystemID Column: شناسه سیستم خطا، منبع خطا, نوع داده integer.
 | 
GateWayes.GateWayID Column: شناسه، کلید اصلی، منبع، شناسایی، دروازه, نوع داده integer.
 | 
GateWayes.GateWayName Column: نام، دروازه، شناسایی، متن, نوع داده varchar.
 | 
OrderDetails.OrderDetailID Column: شناسه جزئیات سفارش، کلید اصلی, نوع داده integer.
 | 
OrderDetails.OrderID Column: شناسه سفارش، شناسایی، سفارش، کلید خارجی, نوع داده integer.
 | 
OrderDetails.BookID Column: شناسه کتاب، کد کتاب، شناسایی کتاب, نوع داده integer.
 | 
OrderDetails.Price Column: قیمت، مبلغ، واحد، عددی, نوع داده decimal.
 | 
Orders.OrderID Column: شناسه سفارش، منحصربه‌فرد، کلید اصلی, نوع داده integer.
 | 
Orders.UserId Column: شناسه کاربر، خریدار، مشتری, نوع داده varchar.
 | 
Orders.OrderDate Column: تاریخ سفارش، زمان خرید، ثبت تاریخ, نوع داده datetime.
 | 
Orders.terminalNumber Column: شماره ترمینال، دستگاه پرداخت، شناسایی تراکنش, نوع داده varchar.
 | 
Orders.responseCode Column: کد پاسخ، وضعیت سفارش، سیستم, نوع داده varchar.
 | 
Orders.paymentId Column: شناسه پرداخت، تراکنش، پرداخت، اطلاعات مالی, نوع داده varchar.
 | 
Orders.RequestId Column: شناسه درخواست، منحصربه‌فرد، پیگیری سفارش, نوع داده varchar.
 | 
Orders.rrn Column: شناسه سفارش، کد منحصر، ردیابی, نوع داده varchar.
 | 
Orders.amount Column: مبلغ، قیمت، ارزش، پول, نوع داده decimal.
 | 
Orders.maskedPan Column: شماره کارت، شناسایی، مشتری، پرداخت, نوع داده varchar.
 | 
Orders.traceNo Column: شماره پیگیری، ردیابی سفارش، منحصربه‌فرد, نوع داده varchar.
 | 
Orders.IsComittedBySimac Column: تعهد، سیماک، وضعیت، خرید, نوع داده boolean.
 | 
Orders.FullName Column: نام کامل، مشتری، فرد، شناسایی, نوع داده varchar.
 | 
Orders.NationalId Column: کد ملی، شناسایی مشتری، منحصر به فرد, نوع داده varchar.
 | 
SiteSetting.SettingId Column: شناسه تنظیمات، کلید اصلی، عدد صحیح, نوع داده integer.
 | 
SiteSetting.Name Column: نام، عنوان، تنظیمات، سایت, نوع داده varchar.
 | 
SiteSetting.Value Column: مقدار، عدد صحیح، تنظیمات سایت, نوع داده integer.
 | 
Tokens.TokenID Column: شناسه توکن، کلید اصلی، عدد صحیح, نوع داده integer.
 | 
Tokens.TokenValue Column: مقدار توکن، رشته متنی، داده‌ها, نوع داده varchar.
 | 
Tokens.CreateTime Column: زمان ایجاد، تاریخ، ساعت، ثبت زمان, نوع داده datetime.
 | 
Tokens.UserId Column: شناسه کاربر، مالک توکن، هویت کاربری, نوع داده varchar.
 | 
Relation: OrderDetails.BookID ↔ Book.BookID. نوع رابطه many-to-one. 
 | 
Relation: OrderDetails.OrderID ↔ Orders.OrderID. نوع رابطه many-to-one. 
 | 
Relation: Tokens.UserId ↔ AspNetUsers.Id. نوع رابطه many-to-one. 